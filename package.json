{
  "name": "html-css-ts-project-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "vite",
    "predeploy": "npm run build",
    "deploy": "netlify deploy --prod --dir=dist"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "vite": "^3.2.5",
    "vite-plugin-checker": "^0.5.6"
  },
  "prettier": {
    "trailingComma": "es5",
    "tabWidth": 2,
    "semi": false,
    "singleQuote": true,
    "useTabs": false
  },
  "dependencies": {
    "save-dev": "^0.0.1-security"
  }
}
